package model.entity.enemy;

public class CaveGoblin extends Enemy {
    public CaveGoblin(String name, int minDamage, int maxDamage, int maxHealth, int gold) {
        super(name, minDamage, maxDamage, maxHealth, gold);
    }
}
