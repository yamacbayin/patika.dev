package model.entity.enemy;

public class WaterNymph extends Enemy {

    public WaterNymph(String name, int minDamage, int maxDamage, int maxHealth, int gold) {
        super(name, minDamage, maxDamage, maxHealth, gold);
    }
}
