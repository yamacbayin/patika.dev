package model.entity.enemy;

public class ForestTroll extends Enemy {

    public ForestTroll(String name, int minDamage, int maxDamage, int maxHealth, int gold) {
        super(name, minDamage, maxDamage, maxHealth, gold);
    }
}

