package model.entity.enemy;

public class DwarvenMiner extends Enemy {

    public DwarvenMiner(String name, int minDamage, int maxDamage, int maxHealth, int gold) {
        super(name, minDamage, maxDamage, maxHealth, gold);
    }
}
