package model.entity.character;

public class Knight extends Character {
    public Knight(int minDamage, int maxDamage, int maxHealth, int gold, String name) {
        super(minDamage, maxDamage, maxHealth, gold, name);
    }
}
