package model.entity.character;

public class Wizard extends Character {
    public Wizard(int minDamage, int maxDamage, int maxHealth, int gold, String name) {
        super(minDamage, maxDamage, maxHealth, gold, name);
    }
}
