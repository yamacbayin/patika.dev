package model.entity.character;

public class Archer extends Character {
    public Archer(int minDamage, int maxDamage, int maxHealth, int gold, String name) {
        super(minDamage, maxDamage, maxHealth, gold, name);
    }
}
